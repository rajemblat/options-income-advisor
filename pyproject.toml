[project]
name = "options-income-advisor"
version = "0.1.0"
description = "Motor de análisis y alertas de opciones para el escenario de Ingreso a Largo Plazo (Fase 1)"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "tenacity>=8.2",
    "py_vollib>=1.0.1",
    "pandas>=2.2",
    "numpy>=1.26",
    "APScheduler>=3.10",
    "pandas_market_calendars>=4.4",
    "pydantic>=2.7",
    "PyYAML>=6.0",
    "streamlit>=1.35",
    "plotly>=5.22",
    "anthropic>=0.34",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2",
    "pytest-mock>=3.14",
    "freezegun>=1.5",
]
# Fuente GRATIS de respaldo para las fechas de earnings (market_context/earnings.py). El código lo
# importa dentro de un try/ImportError, así que sin esto el robot funciona igual — pero pierde ese
# dato en silencio, y las fechas de earnings son las que evitan vender prima encima de un balance.
# Se declara como extra, no como dependencia obligatoria, para que esa relación quede escrita:
# es opcional para arrancar, deseable para operar. La instalación del servidor lo incluye.
yahoo = [
    "yfinance>=1.5",
]

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
